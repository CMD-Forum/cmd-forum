"use client";

export default function ComIDBtn() {
    return (
        <button className={"navlink-full"} onClick={() => null}></button>  
    );
}

export function ComMemBtn() {
    return (
        <button className={"navlink-full"} onClick={ () => null}></button>  
    );
}